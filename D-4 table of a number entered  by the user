#include <stdio.h>

int main()
{
    int table,n;
    printf("Enter n :");
    scanf("%d",&n);
    for(int i=1;i<=10;i++)
    {
       table=n*i;
       printf("%d X %d = %d\n",n,i,table);
    }

    return 0;
}
